package LinkedList;

public class RemoveDuplicate {
    public static void main(String[] args) {
        LinkedList ls = new LinkedList();
        ls.insertAtLast(2);
        ls.insertAtLast(2);
        ls.insertAtLast(2);
        ls.insertAtLast(2);
        ls.insertAtLast(2);
        ls.display();

    }

}
